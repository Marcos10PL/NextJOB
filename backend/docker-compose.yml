services:
  db:
    image: postgres:latest
    container_name: postgres2
    env_file:
      - .env
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data:
